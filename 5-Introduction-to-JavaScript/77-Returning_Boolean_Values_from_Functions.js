//In this exercise I learned that instead of testing a condition to be returned
//with an if/switch statement, the comparison can just be returned

function isLess(a, b) {
  // Fix this code

  return a <= b;
}

// Change these values to test
isLess(10, 15);
